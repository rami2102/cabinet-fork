# Progress — Amazingg KB

[2026-03-21] Project scaffolded with Next.js 16, TypeScript, Tailwind CSS, shadcn/ui. Installed core deps: Tiptap, Zustand, gray-matter, turndown, next-themes, unified/remark.

[2026-03-21] Built file-backed storage layer (path-utils, fs-operations, page-io, tree-builder) with path traversal prevention. Created CRUD API routes for tree and pages.

[2026-03-21] Built Zustand stores (tree-store, editor-store), API client, Tiptap WYSIWYG editor with formatting toolbar, and markdown roundtrip via HTML intermediate (remark→HTML→Tiptap, Tiptap→HTML→turndown).

[2026-03-21] Built collapsible tree sidebar with expand/collapse persistence in localStorage. Added "New Page" dialog for creating pages at root level. Built app shell layout with header and dark/light theme toggle.

[2026-03-21] Added drag-and-drop reordering in tree sidebar with move API endpoint (PATCH). Pages can now be dragged onto folders to nest them. Added "Add Sub Page" option to right-click context menu on every tree node.

[2026-03-21] Upgraded typography from Geist to Inter + JetBrains Mono. Refined letter-spacing, font sizes, and heading weights across editor and sidebar to match clean shadcn/ui aesthetic.

[2026-03-21] Added AI Editor panel on right side. "How would you like to edit this page?" prompt at bottom of every page opens a chat interface that sends instructions to Claude CLI (`claude -p`) to live-edit the current document. Toggle via sparkle icon in header.

[2026-03-21] Created CLAUDE.md with project context, architecture, rules, and instruction to update PROGRESS.md after every change. Created PROGRESS.md as the project changelog.

[2026-03-21] Added paste/drag-drop media upload with image insertion in editor. Built upload API endpoint and static asset serving route for uploaded files.

[2026-03-21] Built full-text search with Cmd+K dialog. Server-side search across all markdown files with title/snippet/path display and arrow key navigation.

[2026-03-21] Added export dropdown in header: Copy Markdown, Copy as HTML, Download .md. Added search hint button with ⌘K keyboard shortcut display.

[2026-03-21] Added embedded website detection — directories with index.html (no index.md) show globe icon in tree and render in iframe with toolbar (open in new tab).

[2026-03-21] Integrated simple-git for auto-commit on save (5s debounce), version history API (git log per page), diff viewer endpoint, and manual commit support.

[2026-03-21] Built kanban task board with 4 columns (Backlog, In Progress, Review, Done). Drag-drop cards between columns, inline add task, YAML-backed storage in /data/tasks/board.yaml.

[2026-03-21] Built Claude Code web terminal with xterm.js + node-pty via standalone WebSocket server. Terminal panel toggleable from header, supports maximize/minimize.

[2026-03-21] Built agent task runner with "Run with Agent" button on task cards, in-memory session manager, and Agent Dashboard with live stats and session control.

[2026-03-21] Built scheduled jobs engine with node-cron, YAML job configs in /data/.jobs/, Jobs Manager UI with create/toggle/run-now/delete, and run history tracking.

[2026-03-21] Added PDF export via client-side html2canvas + jsPDF. Available in the export dropdown menu.

[2026-03-21] Added global keyboard shortcuts: Cmd+S (save), Cmd+K (search), Cmd+` (terminal), Cmd+Shift+A (AI panel). Added 4-tab sidebar navigation: Pages, Tasks, Agents, Jobs.

[2026-03-21] Populated competitors page with 25 competitors across 4 categories: GPU Cloud Providers, GPU Optimization/Orchestration, AI Inference Optimization, and Hyperscalers. Added timestamped changelog entry.

[2026-03-21] Added Tiptap table support (table, row, cell, header extensions) with CSS styling. Added turndown-plugin-gfm for markdown table roundtrip.

[2026-03-21] Added Rename option to tree context menu with rename API, slug generation, and frontmatter title update.

[2026-03-21] Added markdown source toggle button in editor toolbar. Switch between WYSIWYG and raw markdown editing with live sync.

[2026-03-21] Created CLAUDE.md at KB root (/data/CLAUDE.md) to teach agents about KB structure, conventions, frontmatter format, and page creation rules.

[2026-03-21] Added version history panel — click clock icon in header to see git log for current page, click any commit to view colored diff (green=added, red=removed).

[2026-03-21] Added /api/agents/headless endpoint for one-shot AI operations (auto-link, review, summarize) with captured output.

[2026-03-21] Built provider abstraction layer: AgentProvider interface, ProviderRegistry singleton, Claude Code Max provider plugin. /api/agents/providers endpoint for health checks. Future providers plug in via registry.

[2026-03-21] Mobile-responsive layout: sidebar auto-collapses on mobile with overlay backdrop, responsive editor padding, horizontally scrollable toolbar.

[2026-03-21] Wiki-links support: type [[Page Name]] in editor to create styled internal links. Custom Tiptap mark extension with input rule, turndown serialization back to [[syntax]].

[2026-03-21] Provider settings page: view CLI/API agent providers with health status, default provider indicator, placeholder for future Gemini/Codex/API providers. Accessible via gear icon in sidebar.

[2026-03-21] Slash commands: type / in editor for command palette with heading, list, code block, blockquote, divider, table, and image commands. Arrow key navigation and search filtering.

[2026-03-21] Dark/light theme polish: fixed wiki-link CSS for cross-browser compatibility, verified CSS variable-based theming works in both modes.

[2026-03-21] Hidden CLAUDE.md from tree sidebar (system file). Added My Tasks date view with Board/List toggle — tasks grouped by Overdue, Today, This Week, Later, No Due Date.

[2026-03-21] Added global status bar at bottom showing save status and uncommitted git changes count. Polls git status every 15s.

[2026-03-21] Added @ file mention in AI panel — type @ to see dropdown of all KB pages, select to attach as context. Selected pages' content is fetched and injected into the Claude prompt. Tags shown above input, removable.

[2026-03-21] Added 5 companies (Impala AI, 2 Delta, Atero, Tavily, Nebius) to competitors list in new "Other Competitors" section. Created separate sub-pages for each under data/companies/competitors/.

[2026-03-21] Fixed AI edit prompt — Claude now edits files DIRECTLY using its tools (read/edit) instead of returning full content as stdout. Prevents content replacement. AI panel shows Claude's summary of what changed.

[2026-03-21] Added "Restore this version" button in Version History diff view. Restores page to any previous git commit state with confirmation dialog. Updated CLAUDE.md with AI editing behavior rules and full feature list.

[2026-03-21] Added checklists/task lists (TaskList + TaskItem extensions) with checkbox styling, strikethrough for completed items. Added to toolbar and slash commands.

[2026-03-21] Added code block syntax highlighting via CodeBlockLowlight with lowlight common languages. Keyword, string, number, comment, function styles.

[2026-03-21] Added callout blocks (info, warning, success, error) with colored left border and background. Available via /callout and /warning slash commands.

[2026-03-21] Enhanced search with tag filtering — results show tags, clicking a tag filters by it. Search API supports ?tag= parameter. Tags shown as clickable chips.

[2026-03-21] Added visual cron builder in Jobs Manager — frequency picker (hourly/daily/weekly/monthly) with hour selector. Raw cron still editable below.

[2026-03-21] Added job post-actions (on_complete/on_failure) with git_commit action and template variable substitution ({{date}}, {{job.name}}, {{job.id}}, {{job.workdir}}).

[2026-03-21] Added NVIDIA H100 GPU guide page under gpu-knowledge/ covering specs, Hopper architecture highlights, A100 comparison, deployment configs, cloud availability, software ecosystem, and pricing.

[2026-03-21] AI auto-linking: "Auto-link" button on task cards sends title/description to Claude which returns relevant KB page paths. Linked pages shown as chips on cards.

[2026-03-21] Smart search fallback: when text search returns no results, "Ask AI" button appears. Uses headless Claude to find relevant content in the KB.

[2026-03-21] Multi-session terminal: terminal panel now supports multiple tabs, each running an independent Claude Code session. Add/close tabs, switch between them.

[2026-03-21] Daily digest endpoint (/api/agents/digest): summarizes yesterday's git activity + task status into a concise markdown digest via Claude.

[2026-03-21] Post-completion auto-summarize: when an agent finishes a task, git diff stats are automatically appended to the session output as a summary.

[2026-03-21] Live agent terminal view: "Run with Agent" on task cards now opens a terminal tab showing the full Claude Code session live — prompts, tool calls, file edits all visible in real-time. Agent tabs marked with bot icon.

[2026-03-21] AI panel now has two send modes: Send (quick headless edit) and Terminal (opens full Claude Code session in a tab for complex edits). Updated PRD Phase 4 with GitHub OAuth auth design and git-based user tracking.

[2026-03-21] Imported Google Drive docs (StartupDocsDrive) into KB. Converted 17 .docx and 3 .html files to markdown via pandoc. Reorganized into clean structure: product/ (formulation, roadmap, validations, archive), study/ (APEX, lecture notes, competitors), presentations/ (pitch decks). Fixed typos, slugified names, added frontmatter, created index pages.

[2026-03-21] Major KB reorganization into startup operating system structure. New top-level: product/ (formulation, roadmap, archive), market/ (competitors with merged duplicate content, landscape, segments), people/ (CRM with team, advisors, investors, industry contacts, potential customers), research/ (gpu-hardware, technical deep-dives, validations, sessions), operations/ (fundraising), presentations/. Split roadmap doc into clean roadmap + people CRM entries. Updated getting-started with current feature list. Removed old companies/, gpu-knowledge/, study/ dirs.

[2026-03-22] Added full-screen app embedding support. New "app" node type detected via .app marker file in website dirs. When clicked, sidebar and AI panel auto-collapse for immersive full-screen iframe experience. "Back to KB" button restores layout. Moved sidebar collapsed state to Zustand store for cross-component control. Copied GPU Visual Emulator into data/gpu-emulator/.

[2026-03-22] Fixed wiki-links not rendering in editor — added pre-processing step in MD→HTML conversion to transform [[Page Name]] syntax to proper anchor tags before remark processes the markdown.

[2026-03-22] Fixed task list checkboxes rendering incorrectly — added post-processing step in MD→HTML to convert remark-gfm's standard checkbox HTML to Tiptap's expected DOM structure (data-type, data-checked, label wrapper).

[2026-03-22] Fixed hydration mismatch in sidebar — separated mounted state from isMobile detection, added suppressHydrationWarning, moved sidebar collapsed state to Zustand store.

[2026-03-22] Created GTM outbound target list at data/market/gtm-targets.csv with 50 companies across NeoCloud/GPU Cloud, AI Infrastructure, Enterprise GPU Workloads, and Edge AI segments. Each entry includes Why Target, GPU Pain Point, Entry Strategy, and Priority (P0/P1/P2) columns. 11 P0 targets identified with warm intro paths via Israeli network and Apple alumni connections.

[2026-03-22] Added AI task review & enrichment. New /api/agents/review endpoint sends task to Claude for review. Returns structured suggestions: improved description, tags, priority (P0/P1/P2), estimated effort, acceptance criteria, and strategic advice. "Review" button on task cards shows inline suggestion panel with Apply/Dismiss. Completes Phase 3.

[2026-03-22] Implemented autonomous agent system (Phase 4c). Agents defined as markdown files in /data/.agents/ with YAML frontmatter (heartbeat schedule, budget, focus areas). Heartbeat executor loads persona + memory + inbox, runs Claude headless, parses structured memory updates and inter-agent messages. Memory system: context.md (rolling), decisions.md (append-only), learnings.md (persistent). Agent-to-agent messaging via .messages/ inbox. 5 pre-built templates: CEO, Founding Engineer, Growth Marketer, Research Analyst, Operations. Agent Dashboard extended with Team tab showing all personas with expand/collapse detail (memory, inbox, heartbeat history). API routes for persona CRUD, heartbeat trigger, memory access, toggle active/paused.

[2026-03-22] Major UI simplification: removed 4-button tab strip from sidebar. Tasks, Agents, and Jobs now appear as tree sections at the top of the sidebar. Agents section is collapsible with General + all persona agents listed. Clicking an agent opens a Claude Code-style session view with sessions/memory/inbox panel on the left and full session output on the right. Manual prompt input at the bottom of each agent. Replaced ViewMode enum with unified selectedSection model.

[2026-03-22] Fixed Edge runtime crypto error in middleware — replaced Node.js `crypto` module with Web Crypto API (`crypto.subtle.digest`) in middleware.ts and both auth API routes. Password protection now works with KB_PASSWORD env var.

[2026-03-26] Added auto-sync from GitHub. On page load, the app automatically pulls latest changes from the remote. Added "Sync" button in the status bar that pulls + refreshes the tree. New `/api/git/pull` endpoint and `gitPull()` in git-service.

[2026-03-26] Fixed sidebar tree: leaf pages (directories with no children) now show a page icon instead of a folder icon and are no longer expandable. Only nodes with actual children show the chevron arrow and folder icon.

[2026-03-29] Added RTL support. New `dir` field in frontmatter (`rtl` or omitted for LTR). Toggle button in editor toolbar switches direction per page. Applied to both WYSIWYG and source mode views.

[2026-03-29] Fixed page flicker bug. Editor was fetching each page twice on navigation — a redundant useEffect re-called loadPage when currentPath changed. Removed the duplicate effect; content now loads once per page switch.

[2026-03-29] Enriched competitor landscape from talk notes. Extracted companies from all 7 talks, enriched 3 existing pages (Nebius, Zymtrace, Impala AI), created 8 new competitor pages (Run:AI, ClearML, Together AI, Fireworks AI, Highrise.ai, Infra.is, TStable, PrePeak). Updated competitors index with new ecosystem section and market insights. Updated competitors.csv with 17 entries.

[2026-03-29] Fixed Claude Code web terminal — full rich output now works. Root cause: node-pty spawn-helper lacked execute permissions and had macOS quarantine attributes, causing `posix_spawnp` to fail. Fixed terminal server to resolve claude binary path, added error handling so server doesn't crash on spawn failure, added postinstall script to auto-fix permissions. Enhanced xterm.js with Unicode 11, web links addon, richer color theme, and 10k scrollback. Made terminal panel resizable via drag handle.

[2026-03-29] Upgraded AI Editor panel from headless `claude -p` to full embedded Claude Code terminal. Each edit request now spawns a real PTY session rendered inline via xterm.js, showing tool calls, file diffs, streaming output, and the interactive prompt — same experience as the bottom terminal. Removed the old headless spinner UI. Panel auto-reloads the page when Claude finishes editing. Added clear sessions button and widened panel to 480px.

[2026-03-29] Added comprehensive Claude Code integration architecture section to PRD (section 4.6.2a). Documents both execution modes (PTY interactive vs headless pipe), component-to-mode mapping table, architecture diagram, key files reference, modification guide, and the node-pty macOS spawn-helper gotcha.

[2026-03-29] AI Editor sessions now persist across navigation. Created "Editor Agent" persona (`data/.agents/editor.md`) — all AI Editor sessions are recorded to JSONL history files and appear in the Agents panel under "Editor Agent". Terminal server captures output per session and exposes it via HTTP endpoint. Sessions saved via `sendBeacon` on page navigation for reliability. Past sessions load as collapsible summaries when returning to a page.

[2026-03-29] AI Editor sessions survive page navigation. Moved session state from React useState to Zustand store (`ai-panel-store.ts`). WebTerminal instances for other pages stay mounted (hidden) so WebSocket connections stay alive. When navigating away, the AI panel shows "Running on other pages" with spinner. Returning to the page shows the live terminal again. Editor Agent panel in Agents shows live running sessions with spinner and page label. Terminal server exposes GET /sessions endpoint for active session listing.

[2026-03-29] Editor Agent panel now shows clickable sessions with full Claude Code output. Running sessions show a live terminal embed in the main content area. Completed sessions fetch and display full captured output (not just summary). History sessions show instruction text + page path instead of just timestamps. Full output field added to editor-sessions API (stored in JSONL, stripped from list responses, fetched individually). Fixed React hooks ordering error (useCallback must be before early return).

[2026-03-29] Created full POC 6 (Compound AI Workflow) page with goal, hypothesis, success KPIs, 3-model pipeline design (Router/Specialist/Verifier), detailed implementation in 4 phases (multi-model serving setup, pipeline orchestrator, benchmark suite, latency breakdown), expected results table, investor pitch framing, risks & mitigations, and deliverables. All 6 POC pages now complete with detailed specs.

[2026-03-30] Multi-theme system with 11 custom themes. Click theme button to toggle dark/light, long-press (or right-click) to open theme picker menu. Themes: Claude (warm brown, Space Grotesk), Midnight Ocean (deep blue, DM Sans), Aurora (purple, Outfit), Ember (orange, Sora), Forest (green, Plus Jakarta Sans), Cyber (cyan, Space Mono), Paper (sepia, Merriweather Sans), Sakura (pink, Nunito), Meadow (green, Rubik), Sky (blue, Figtree), Lavender (purple, Quicksand). Each theme sets OKLCh CSS variables + custom Google Font. Persists to localStorage. Theme picker shows colored accent dots and checkmarks.

[2026-03-30] Agent sessions now survive page refresh. Terminal server keeps PTY processes alive when WebSocket disconnects (detach instead of kill), allows reconnection by session ID with full output replay. Running sessions are persisted to sessionStorage and restored on page load. AI Panel validates restored sessions against the terminal server on mount, cleaning up any that finished while the page was refreshing. Also fixed pre-existing xterm windowsMode TS error.

[2026-03-31] Wrote comprehensive PRD for Playbooks & Multi-Agent System at product/playbooks-prd. Covers: Plays (atomic skills) + Playbooks (collections) + Agents (personas with memory/goals/workspaces), Department Lead orchestrators, Agent Slack with auto-channels, Mission Control dashboard, goal-driven autonomy with KPI tracking, agent workspaces as KB output (reports, apps, data, code), SQLite for hot data (Slack messages, task queue, session logs) while keeping goals/content as files, fully autonomous agents that write/execute code and publish results. 6-phase implementation plan.

[2026-03-31] Added scheduling & triggers design (section 5.6) to PRD. Evolves existing jobs system (node-cron + YAML) into friendly agent scheduling: two layers (agent heartbeat + per-play schedules/triggers), human-readable presets replacing raw cron, natural language schedule input ("every 4 hours on weekdays"), event triggers (file_changed, webhook, agent_message, goal_behind, on_complete), schedule picker UI with quick picks + preview of next 3 runs, smart goal-aware scheduling where agents can adjust their own frequency. Cron stays as internal engine; UI makes it friendly.

[2026-03-31] Added detailed Main Page Design spec at product/main-page-design. Three-zone layout: Company Pulse (6 key metrics strip), Agent Grid (department cards with goal progress bars + emoji avatars), Agent Slack (resizable bottom panel with channel tabs). Includes agent detail slide-over, three creation paths (from playbook, natural language, from scratch), responsive behavior, keyboard shortcuts, and SSE-based real-time state management. Also added Claude Code architecture alignment section to PRD showing how Cabinet concepts map 1:1 to Claude Code sub-agents, agent teams, skills, plugins, memory, and hooks. PRD updated with deployment architecture (4 processes), error handling (self-heal first), notifications (browser push + Telegram + Slack + email), and MVP definition (full Marketing department). Decisions: SQLite now/Postgres later, HTTP+WebSocket IPC, web-based setup, emoji avatars, "Cabinet Agents" branding.

[2026-03-30] Added Cabinet Agents backend modules: slack-manager.ts (file-based JSONL messaging with per-channel storage, post/read/list/system-message functions) and goal-manager.ts (goal state tracking with increment, period reset, pace-based status computation, and summary aggregation).

[2026-03-30] Created 3 marketing department agent files using evolved agent format: Head of Marketing (lead, daily heartbeat, coordinates team), Content Agent (specialist, 15min heartbeat, Reddit + LinkedIn plays), Outreach Agent (specialist, 4-hour heartbeat, lead scoring + competitor tracking). Each has department, type, plays, goals with metrics/targets, channels, and workspace directories. Also created .slack/ and .playbooks/ directory structure.

[2026-03-30] Created play-manager.ts (list, read, write, delete, execute, history) following persona-manager patterns. Added 4 play definition files: reddit-monitor (4h interval, subreddit scanning + response drafting), linkedin-poster (daily at 9am, content publishing), lead-scorer (manual + on_complete trigger, ICP scoring), competitor-tracker (4h interval, website/social monitoring). Plays stored in data/.playbooks/plays/ with YAML frontmatter, history in data/.playbooks/.history/ as JSONL.

[2026-03-31] Wired plays into heartbeat system: heartbeat.ts now loads assigned plays (via play-manager), goal state (via goal-manager), includes both in the agent prompt, parses SLACK messages from agent output and posts them to Agent Slack channels, auto-posts heartbeat summary to the agent's primary channel.

[2026-03-31] Created Create Agent dialog (create-agent-dialog.tsx) with emoji picker, department/type selectors, schedule-picker.tsx (friendly cron replacement with quick-pick buttons, human-readable display, next-3-runs preview, raw cron toggle), and play assignment checkboxes. Wired to "+ New Agent" button and create card in Mission Control. Enhanced individual agent pages: added Goals tab (progress bars with color-coded status), Plays tab (assigned plays list), emoji avatar in header, department badge.

[2026-03-31] Merged Jobs into Plays — killed the Jobs system. Jobs and Plays were the same concept (a prompt that runs on a schedule via Claude CLI). Converted the existing "Daily Competitor analysis" job to a play (daily-competitor-analysis.md). Rewrote jobs-manager.tsx as a plays-only view with Create Play dialog using the schedule picker. Renamed sidebar "Jobs" to "Plays". One system, one format (markdown), one UI.

[2026-03-31] Implemented Phase 1 of Cabinet Agents MVP. Backend: evolved AgentPersona interface with emoji/department/type/goals/plays/channels fields (backward-compatible), created types/agents.ts with GoalMetric/PlayDefinition/SlackMessage types, play-manager.ts, slack-manager.ts (JSONL-based), goal-manager.ts. Data: 3 marketing department agents (head-of-marketing, content-agent, outreach-agent) + 4 plays (reddit-monitor, linkedin-poster, lead-scorer, competitor-tracker). API routes: /api/agents/slack (GET/POST), /api/plays (GET/POST), /api/plays/[slug] (GET/PUT/DELETE/POST trigger). UI: Mission Control section wired into app-store + app-shell + sidebar (Gauge icon), mission-control.tsx with PulseStrip (6 metrics), DepartmentCard grid (grouped agents with goal progress bars), AgentCard (emoji avatars + inline goals), GoalBar (color-coded progress), SlackPanel (channel tabs + message feed + human input). Build passes.

[2026-03-31] Agent detail slide-over panel now opens when clicking any agent in Mission Control — overlays on top of MC with dark backdrop showing Goals (with progress bars), Plays (with Run Now), Workspace (file browser), Memory, and Recent Activity. "Full View" button navigates to the full agent session view. Escape key closes the panel. Added Cmd+M keyboard shortcut to toggle Mission Control. Made Slack panel resizable via drag handle (min 80px, max 600px). Default Slack channels (#general, #marketing, #alerts) now always show even before any messages exist. Create Agent dialog now auto-creates workspace/ directory for new agents. Workspace API filters hidden files (dotfiles).

[2026-03-31] Made Agent Slack functional: fixed message ordering (chronological oldest-first for chat display), enriched Slack messages with agent emoji + display name (stored in JSONL, shown in UI with colored type badges for reports/alerts), merged goal state from disk into persona API (goals now reflect actual progress from goal-manager, not static frontmatter defaults), added human @mention routing (typing @agent-slug in Slack delivers the message to that agent's inbox for next heartbeat processing).

[2026-03-31] Mission Control polish + goal/play wiring. Visual: added distinct emojis to all agents (CEO 👔, Founding Engineer 🛠, Growth Marketer 📈, Operations ⚙️, Research Analyst 🔬, Editor ✏️), filtered out non-agent files (loop-prompt) from persona list, changed cost metric from "—" to "$0". Play execution history: added history endpoint to /api/plays?history=true, Plays page now loads and displays execution history per play (last run status badge, run count) plus a unified Execution History section with timestamps, agent tags, duration, and summaries. Goal wiring: heartbeat.ts and play-manager.ts now parse GOAL_UPDATE directives from agent output and call updateGoal() to increment metrics, added updateGoal API action to persona route, pulse strip playsThisWeek metric now reads from real play history data. Enhanced agent status indicators: cards now show "Paused" (orange pause icon) for inactive agents, "Live" (solid green) for recently active, "Idle" (dim green) for active but waiting. Made workspace files clickable in agent detail panel — clicking navigates to the file in the KB editor. Standalone agents (department=general) now render as individual cards in the grid instead of being grouped under a "General" department card, matching the PRD design. Per-play "Run" buttons in the agent detail panel (hover to reveal) with loading spinner states. "Run All" button triggers full heartbeat. Added Cmd+/ keyboard shortcut to focus Agent Slack input when in Mission Control. Wired pulse strip alerts count to real #alerts channel data. Added Slack channel badge counts (message count badges on non-active channel tabs, red badge for #alerts).

[2026-03-31] Mission Control enhancements round 2. Bug fixes: empty state "No agents configured" now correctly checks both departments AND standalone agents (was hiding standalone agents). Features: Agent cards show "Next: Xm" countdown for idle agents (cron-based computation in persona-manager.ts via new computeNextCronRun). Agent Slack renders inline markdown (**bold**, `code`, [links](url), @mentions) with styled output. Create Channel (+) button in Slack with inline name input. Slack panel collapsible via double-click drag handle or Cmd+Shift+A. Natural language agent creation: "Describe It" card path opens dialog, Claude generates agent config from description. "Playbooks" button in MC header navigates to Plays page. Agent cards show last action text from recent Slack messages. Agent detail has "Edit" button to open .md definition in KB editor. Floor alerts: heartbeat checks goal floors, auto-posts to #alerts when below floor with 80%+ period elapsed. GoalBar renders floor threshold as vertical red marker. Cmd+N opens Create Agent dialog in MC. Pulse strip metrics are clickable (Alerts → switches Slack to #alerts, Plays this week → navigates to Plays page). Slack panel listens for channel-switch events from Pulse strip. Header updated to "Cabinet — Your Company OS".

[2026-03-31] Built Onboarding Wizard (PRD Section 10). First-time users see a 3-step guided setup: Step 1 — Company Profile (name, product, team size), Step 2 — Department Selection (9 departments: Marketing, Sales, Engineering, Content, Research, Operations, Support, Hiring, Finance with icons and descriptions), Step 3 — Review & Launch (shows agent team tree with emoji avatars, play assignments, Slack channels, and runtime). Wizard auto-detects first-time setup via /api/agents/config endpoint (reads/writes data/.agents/.config/company.json). On launch, creates agent personas with workspace directories for all selected departments. After completion, redirects to Mission Control. Subsequent visits skip the wizard and load the normal app.

[2026-03-31] Playbook Catalog + Slack Threads + Shortcuts. Built "From Playbook" agent creation path — the 3-step catalog flow: Step 1 picks a category (9 categories with icons), Step 2 browses/selects plays with search and checkboxes, Step 3 configures agent name/emoji/schedule and launches. Added as third option on the Create Agent card alongside "From Scratch" and "Describe It". Added Slack thread reply support: clicking any message shows a "Reply" button, thread replies are stored with parent message ID, thread view filters to show only the thread with a back button, reply counts shown on messages with replies. Added Cmd+1-9 keyboard shortcuts to jump to agent cards by position in Mission Control. Agent cards now have data-agent-card attributes for programmatic selection.

[2026-03-31] Agent detail panel improvements. "Run All" now auto-activates paused agents before triggering heartbeat. Polls for completion by watching heartbeat history length (5min timeout). Provides better feedback during long-running heartbeats.

[2026-03-31] Active heartbeat tracking + running state in UI. Added running heartbeat tracker to persona-manager (markHeartbeatRunning/markHeartbeatComplete/getRunningHeartbeats). Heartbeat.ts marks agents as running during execution. SSE events endpoint now pushes running state per agent. Agent cards show pulsing green "Running" indicator when a heartbeat is active. Pulse strip "plays running" metric now reflects real running heartbeat count instead of hardcoded 0.

[2026-03-31] Goal summary panel + department card actions + Slack SSE refresh. Clicking "goals on track" in the pulse strip toggles an inline goal summary showing all agents' goals in a 3-column grid with emoji avatars and progress bars. Department cards now have footer actions: "Workspace" and "Start All"/"Pause All" buttons for bulk department control via scheduler API. Slack panel now listens for SSE slack_activity events for near-instant message refresh (polling reduced from 5s to 10s as fallback). Added "Workspace" and "Start All"/"Pause All" buttons at the bottom of department cards (matches PRD design). "Start All" activates all agents in the department and registers their heartbeats; "Pause All" deactivates them. Uses the scheduler API for bulk operations.

[2026-03-31] Agent Scheduler Controls + SSE Real-time Updates. Built scheduler API (/api/agents/scheduler) with bulk start-all/stop-all/activate/pause actions. Added "Start Team" button in MC header with confirmation dialog (warns about API credit usage, shows paused agent count). Clicking "Start Team" activates all paused agents and registers their cron heartbeats. Green "Running (N)" state when scheduler is active, "Pause" to stop all. Added clickable status toggles on agent cards — click "Paused" to activate or "Live"/"Idle" to pause individual agents, works in both department and standalone cards. Built SSE endpoint (/api/agents/events) that pushes agent_status, goal_update, pulse, and slack_activity events every 3s — Mission Control now updates in near real-time instead of polling every 10s (30s fallback poll retained as safety net). Escape key closes confirmation dialog.

[2026-03-31] Mission Control grid layout fix + agent department organization. Fixed CSS grid layout by adding `items-start` so department cards don't force all cards in the same row to match the tallest card's height — standalone agents were hidden below the fold. Organized legacy agents into proper departments: CEO → leadership (lead), Founding Engineer → engineering (lead), Research Analyst → engineering (specialist), Growth Marketer → marketing (specialist), Operations → operations (lead). Added goals to all agents: CEO (strategic reviews, follow-ups), Founding Engineer (tech docs, validations), Research Analyst (reports, specs), Operations (tasks triaged, process docs), Growth Marketer (outreach messages, market updates). Fixed goal bar to not show "critical" status when current=0 (was marking zero progress as red). Added #engineering and #operations Slack channel tabs alongside existing #general, #marketing, #alerts. Pulse strip now shows 0/16 goals total across all departments.

[2026-03-31] Agent detail panel enrichment. Unified activity feed now interleaves heartbeat records AND Slack messages from the agent, sorted chronologically with filter tabs (All / Heartbeats / Messages). Added uptime counter in status strip (computed from first heartbeat). Added goal period label (e.g., "Period: W14") next to goals section header. Added "Open in KB" link to workspace section header. Slack messages show channel name, type badges (report/alert), and colored icons. Activity items have distinct icons for heartbeats (Terminal) vs messages (MessageSquare) vs alerts (AlertTriangle).

[2026-03-31] Clickable KB refs in Slack + browser notifications + company branding. Internal workspace links in Slack messages ([text](workspace/...)) now navigate to the KB page when clicked, using onOpenFile callback wired from MissionControl through SlackPanel. Browser push notifications fire for #alerts channel messages and @human mentions across all channels — SSE events now include message preview and agent info for richer notifications. MC header shows company name from onboarding config (e.g., "Amazingg.ai — Company OS") instead of generic "Cabinet". SSE events endpoint now monitors all department channels (engineering, operations) not just general/marketing/alerts.

[2026-03-31] Activity ticker + expandable heartbeats + session replay + health + cost. Added live activity ticker below the pulse strip showing the latest agent Slack message with emoji, agent name, and timestamp — auto-refreshes on SSE events and every 15s. Heartbeat records in agent detail panel are now expandable — click to toggle between 2-line summary and full session output with chevron indicator. Full session output is saved to per-agent sessions/ directory on each heartbeat, retrievable via API for replay. Agent health indicator computed from recent heartbeat success rate (Healthy/Degraded/Unhealthy) shown in detail status strip. Cost estimation in pulse strip based on plays-per-day average ($0.15/run estimate). Makes Mission Control feel alive and actionable.

[2026-03-31] Interactive pulse strip popovers + agent detail live output. All 6 pulse strip metrics are now clickable with popover panels: "plays running" shows list of currently running agents with pulsing indicators (click to open agent detail); "plays this week" shows a weekly execution log with completion/failure counts, agent emojis, duration, and summaries; "cost today" shows per-agent cost breakdown with proportional bars and total estimate. Agent detail slide-over now has a "Last Output" section styled as a dark terminal box showing the most recent heartbeat output — auto-loads the full session text when the panel opens, shows a pulsing "Running heartbeat..." indicator when active. Popovers dismiss on outside click or Escape key.

[2026-03-31] Goal editor in Create Agent dialog. The "From Scratch" agent creation path now includes an inline goal editor. Users can add multiple goals with metric name, target number, unit label, and period (daily/weekly/monthly). Goals are validated and formatted (spaces→underscores, lowercased) before being saved to the agent's frontmatter. The + Add Goal button shows a form for each goal with remove (X) functionality. Goals are properly merged through writePersona and appear in the agent's MC card immediately after creation.

[2026-03-31] @mention autocomplete in Agent Slack. Typing @ in the Slack input shows a dropdown of all agents with emoji, name, and slug. Arrow keys to navigate, Enter/Tab to select, Escape to dismiss. Selected agents are inserted as @slug format which routes to agent inboxes on send. Agents list loaded once on mount from /api/agents/personas. Dropdown appears above the input bar, positioned at the bottom of the Slack panel.

[2026-03-31] Mission Control visual polish + data seeding. Filtered Editor Agent (system agent) from MC grid and pulse metrics — no longer shows in department cards or agent count. Made agent cards and department cards more compact (reduced padding, font sizes, goal bar height) so all 4 departments + Create Agent card fit with less scrolling. Fixed pulse strip "goals on track" metric to not show as critical when agents have 0 progress (no data yet ≠ behind). Seeded Slack channels (#marketing, #engineering, #operations, #general) with agent welcome/intro messages so Agent Slack feels alive on first load. Created workspace index.md files for marketing agents (Content Agent, Head of Marketing, Outreach Agent) so workspace browsing shows content. Wired department card "Workspace" button to navigate to the lead agent's workspace in the KB editor.

[2026-03-31] Emoji avatars in sidebar + plays for all departments. Added emoji avatars to the sidebar agent list — each agent shows its emoji instead of a plain status dot, with a green active indicator on the right for active agents. Created 4 new plays for non-marketing departments: tech-doc-updater and validation-runner (engineering), task-triager and weekly-digest (operations). Assigned plays to Founding Engineer, Research Analyst, and Operations agents via frontmatter. Plays page now shows 9 defined plays across marketing, engineering, research, and operations categories.

[2026-03-31] Plays page category grouping + Content Agent dashboard. Plays page now groups plays by category with emoji headers (📣 Marketing, 🛠 Engineering, 🔬 Research, ⚙️ Operations) and play counts per category. Created sample Content Dashboard HTML app in Content Agent's workspace (apps/content-dashboard/) — a Plotly-powered dark-themed dashboard showing Reddit replies daily trend, subreddit distribution donut chart, goal progress bars, top keywords chart, 4 KPI metric cards, and a recent thread activity table. Full-screen app with .app marker, demonstrates the PRD's agent-created dashboard concept.

[2026-03-31] Settings page with Integrations & Notifications tabs (PRD Sections 6.3 + 9). Expanded Settings from single provider list to 3-tab layout: Providers (existing CLI/API agents), Integrations (6 MCP server configs — Reddit, LinkedIn, GitHub, Slack, Email, Google Sheets — with toggle switches, command inputs, masked credential fields with reveal buttons, plus Scheduling Defaults with concurrent agent/play limits, active hours, pause-on-error), Notifications (4 channels — Browser Push, Telegram bot, Slack Webhook, Email Digest — each with expandable config fields when enabled, plus Alert Rules section showing always-on triggers). Save button persists config to data/.agents/.config/integrations.json via new API endpoint.

[2026-03-31] Mobile-responsive Mission Control (PRD Section "Responsive Behavior"). Pulse strip switches from 6-column row (desktop) to 3x2 grid (mobile) so all metrics are readable without truncation. Header buttons collapse to icon-only on mobile (play/pause + "+" icons), "Playbooks" and "Refresh" labels hidden on small screens, company subtitle hidden. Consistent responsive spacing/sizing throughout (px-3/py-2 on mobile, px-4/py-3 on desktop). All three breakpoints verified: mobile (390px), tablet (1024px, 2-column grid), desktop (1440px, 3-column grid).

[2026-03-31] Expanded play catalog from 9 to 15 plays (PRD Phase 5: "curated playbook catalog with 10-15 starter plays"). Added 6 new plays across 4 categories: blog-content-writer (content), customer-outreach (sales), market-research (research), code-reviewer (engineering), social-media-scheduler (marketing), ticket-triage (support). Each play has full execution steps, quality rules, triggers, and scheduling. Plays page now shows 15 plays across 7 categories: Content, Engineering, Marketing, Operations, Research, Sales, Support.

[2026-03-31] Goal trend charts + bug fixes. Clicking a GoalBar in the agent detail panel now expands to show an SVG sparkline trend chart with historical goal progress across periods (PRD Section 4.2: "Click goal bar → goal history with trend chart"). Chart shows data points for each past period + current period, a dashed target line, color-coded dots (green=on-track, amber=behind, red=critical, blue=exceeded), period labels, trend indicator (up/down/flat arrow), and historical average. Added getGoalHistory() to goal-manager.ts, goalHistory field to persona API response. Fixed goal status threshold mismatch between goal-manager.ts and goal-bar.tsx (aligned on 0.4/0.2 thresholds). GoalBar now displays unit labels. Seeded goal history data for Content Agent, Outreach Agent, and Growth Marketer with 3 periods of historical data.

[2026-03-31] Fixed pulse strip "goals on track" metric — was counting goals with 0 progress as "on track" (showed 16/16 when most agents had never run). Now excludes goals with no data from both numerator and denominator, showing only goals with actual progress (e.g., 6/6). Made pulse strip consistent with department card goal calculations.

[2026-03-31] Workspace Gallery (PRD Phase 6). New "Gallery" button in MC header toggles a gallery view showing all agent workspace output across agents — apps, reports, data files, and code. Gallery API (/api/agents/gallery) scans all agent workspaces recursively, extracts frontmatter titles and preview text from markdown files, detects HTML apps (.app marker). Gallery UI has type filters (Apps, Reports, Data, Code) and per-agent filters with emoji avatars. Each card shows file type badge, agent attribution, preview text, relative timestamp, and file size. Clicking any item navigates to the KB editor. Uses gray-matter for proper frontmatter-aware preview extraction.

[2026-03-31] Agent export/import (PRD Phase 6: "Playbook import/export"). Export button in agent detail panel downloads a JSON bundle containing the agent's full config (frontmatter + body), all assigned play definitions, and workspace index. Import button on the Create Agent card accepts a JSON bundle file, creates the agent (paused by default for safety), writes any missing play files, and sets up workspace directories. Handles slug conflicts by appending a suffix. Four agent creation paths now available: From Playbook, From Scratch, Describe It, and Import.

[2026-03-31] Notification system (PRD Section 9 + Phase 6). Created notification-service.ts that sends alerts via Telegram bot API and Slack incoming webhooks when configured. Wired into Slack message posting — notifications fire automatically for #alerts channel messages, @human mentions, and agent floor alerts from heartbeats. "Send Test Notification" button added to Settings > Notifications tab for verifying config. Reads credentials from integrations.json config (already managed by Settings UI). Fire-and-forget async delivery.

[2026-03-31] MC grid layout improvement: added 2xl:grid-cols-4 breakpoint so all 4 departments fit in a single row on wide screens (1536px+). At 1680px, Engineering, Leadership, Marketing, and Operations all visible without scrolling.

[2026-03-31] Mission Control polish round 3 + Plays page enhancements. Extracted cronToHuman utility to shared lib/agents/cron-utils.ts (used by schedule-picker and agent detail). Agent detail panel now shows human-readable schedule (e.g., "Every 15 min on weekdays") instead of raw cron expressions. Upgraded "Last Output" section to "Live Terminal" with Run/Kill buttons matching PRD design — empty state shows terminal icon + "Run Heartbeat" CTA, running state shows pulsing animation, completed state shows full output. Department card status badges now correctly show "Paused" (amber) when all agents are paused instead of misleading "Idle", and "Running" (pulsing green) when heartbeats are active. Fixed department card goal counting to exclude goals with no data (was showing all goals as "on track" when they had 0 progress). Plays page now shows "Used by:" agent badges on each play card with emoji + name (loads from /api/agents/personas). Play cards are now expandable — click to reveal full instructions, play-specific execution history, and trigger configuration. Schedule display uses cronToHuman for human-readable fallback. Run/Delete buttons stop event propagation so they don't toggle expand.

[2026-03-31] Event Trigger Engine (PRD Section 5.6). Built trigger-engine.ts — a singleton that evaluates trigger rules and fires play executions in response to events. Supports 5 event types: on_complete (chain plays — when play A completes, fire play B), goal_behind (auto-detect when goals fall behind pace and fire corrective plays), webhook (external HTTP POST triggers a play), file_changed (KB page edits trigger plays watching that path), agent_message (inter-agent messages fire plays). Engine includes debounce (30s per play), duplicate prevention (won't fire already-running plays), and an in-memory trigger log. New API routes: /api/agents/triggers (GET log + POST events), /api/agents/webhook/:slug (external webhook endpoint for each play). Wired on_complete into play-manager.ts (fires after every play execution), file_changed into page PUT handler, and goal_behind check into heartbeat completion. Updated plays with event triggers: reddit-monitor (+goal_behind), tech-doc-updater (+file_changed on product/), code-reviewer and lead-scorer already had webhook/on_complete. Trigger log visible in Plays page (color-coded event type badges per trigger) and agent detail panel (Recent Triggers section). Play cards now show color-coded trigger type badges (manual=gray, schedule=blue, on_complete=purple, webhook=amber, file_changed=green, goal_behind=red).

[2026-03-31] Human commands in Agent Slack now trigger real agent responses (PRD Section 3.3). When a human @mentions an agent in Slack, a lightweight quick-response heartbeat fires immediately (not waiting for next scheduled heartbeat). The agent receives the human message plus recent channel conversation context, goals, and memory, then responds directly in the same Slack channel. If the human sends a message without @mention, the message routes to the department lead for that channel (e.g., #marketing → Head of Marketing, #general → CEO). Added runQuickResponse() in heartbeat.ts (2-min timeout, focused prompt, no memory block parsing). Agent Slack shows an animated "thinking..." typing indicator while the agent generates a response — SSE pushes agent_responding events to the UI which renders bouncing dots with the agent's emoji and name. Typing indicator clears automatically when the response completes.

[2026-03-31] Collapsible department cards + rich Agent Slack seeding. Department cards in MC grid now have a collapse/expand toggle (click header). Collapsed cards show a compact emoji strip of agents instead of full agent cards with goal bars. Departments with 3+ agents start collapsed by default, keeping the grid compact so all departments + Create Agent card are visible with minimal scrolling. Seeded realistic agent activity messages across all Slack channels: #general (6 messages with cross-agent coordination, reports, competitor alerts), #marketing (7 messages showing Reddit scanning, LinkedIn posting, lead scoring, team directives), #engineering (4 messages with tech doc updates and competitor analysis), #operations (1 triage report), #alerts (2 alerts — goal behind pace + competitor alert with @human mentions). MC now feels alive on first load with agent last-action text on cards, populated channel badges, and 2 pending alerts in the pulse strip. Fixed Slack workspace links — internal `[text](workspace/...)` links now resolve to the correct agent workspace path using the message sender's slug (e.g., `workspace/data/reddit-threads.csv` from Content Agent → `.agents/content-agent/workspace/data/reddit-threads.csv`). Changed default landing page from empty "No page selected" editor to Mission Control — Cabinet now opens directly to the MC dashboard on load, matching the PRD vision ("Mission Control IS the full page").

[2026-03-31] Agent Editor Dialog + Delete + Self-Healing + Memory Viewer. Built edit-agent-dialog.tsx — structured form to modify agent config (identity, schedule, goals, plays, system prompt) directly from the agent detail panel's Edit button, replacing the raw .md file navigation. Changes are saved via the persona API with proper merging. Added Delete Agent with confirmation UI in the detail panel footer. Heartbeat self-healing: agents now retry failed heartbeats up to 2 times with backoff before recording failure, auto-post to #alerts on persistent failure, and auto-pause after 3 consecutive failures (PRD Section 8). Workspace auto-index: heartbeat now generates workspace/index.md when workspace has files but no index. Enhanced Memory section with tabbed viewer showing all memory files (context, decisions, learnings) with inline preview. Fixed writePersona to always persist emoji/department/type fields instead of skipping defaults.

[2026-03-31] Play editing + duplication in Plays page. Added Edit Play dialog (title, category, schedule, instructions) and Duplicate Play button to each play card. Edit uses the existing PUT /api/plays/:slug endpoint. Duplicate creates a copy with "-copy" suffix. Play cards now have 4 action buttons: Run, Edit (pencil), Duplicate (copy icon), Delete.

[2026-03-31] Mission Control visual polish round 4 — glanceable health indicators. Color-coded pulse strip metrics: agents metric turns amber when 0 active (warning), green when active; goals metric green/amber/red based on on-track ratio; alerts metric amber for 1-2 alerts, red for 3+; plays-this-week metric green when > 0. Added green "ok" status color to MetricCard (was only amber/red before). Enhanced collapsed department cards: replaced minimal emoji strip with compact agent rows showing emoji + name + inline mini goal progress bars with percentage — departments can now be scanned at a glance without expanding. Added thin color-coded health indicator bar at top of each department card (green=running/healthy, amber=behind, gray=no data). Quick "Run Heartbeat" button on agent cards: green play icon appears on hover, triggers heartbeat with loading spinner, auto-activates paused agents before running. Departments default to collapsed when 3+ departments exist so all cards + Create Agent fit on one screen without scrolling. Added "Expand All / Collapse All" toggle button above the grid for quick view switching.

[2026-03-31] Rich workspace content + working Slack links + agent memory. Created realistic workspace output for all marketing agents: Content Agent has reddit-threads.csv (15 tracked threads), linkedin-drafts.csv (10 posts), weekly-content-report, and keyword-optimizer.py script. Outreach Agent has lead-handoffs.csv (8 scored leads with ICP scores), competitor-weekly report. Head of Marketing has week-14 marketing report. Research Analyst has competitor-alert analysis with credibility assessment table and recommended actions. Populated agent memory (context.md + learnings.md) for Content Agent, Outreach Agent, Head of Marketing, Research Analyst, Growth Marketer, CEO, and Founding Engineer — Memory section in agent detail now shows real learnings and context. Fixed critical workspace link navigation bug: Slack links like "View threads" and "View leads" now correctly navigate to CSV viewer or markdown editor by adding loadPage() call alongside selectPage() in onOpenFile handlers. Fixed app-shell to render CSV/PDF viewers for files not in the tree (e.g., .agents/ workspace files) by inferring file type from extension when selectedNode is null. All Slack workspace references are now clickable and navigate to real content. Created workspace index pages for Research Analyst and Founding Engineer. Fixed Gallery filter tab "Datas" → "Data" pluralization. Gallery now shows 14+ items across 6 agents with working navigation.

[2026-03-31] Full company data seeding — MC now shows active goal progress across ALL departments. Created goal state.json files for CEO (3/5 reviews, 7/10 follow-ups), Founding Engineer (6/8 docs, 2/3 validations), Operations (11/15 tasks, 1/2 docs), Research Analyst (4/5 reports, 3/4 specs), Head of Marketing (22/30 plays, 14/20 leads). Added 4 new Slack messages to #engineering (competitor analysis, POC validation, weekly summary) and 4 to #operations (process docs, task triage, status report). Created workspace index pages for CEO and Operations agents with activity summaries. Seeded play execution history (29 records across 6 plays) for the current week — pulse strip now shows "29 plays this week" and "$0.62 est. today" instead of zeros. All 4 departments now show green progress bars with 50-78% completion instead of 0%. Pulse strip: 16/16 goals on track.

[2026-03-31] UI simplification: removed Tasks kanban board (components, API, store, types — 6 files deleted). Simplified sidebar: removed Tasks button, removed "General" agent entry, cleaned up agent list to show only status dot + emoji + name. Added "Outputs" tab to agent detail page replacing Plays/Inbox tabs — shows files the agent created in KB department folders with click-to-navigate. Enhanced workspace API to scan both agent private workspace and configured output_dir with modification times. Updated PRD (main-page-design) with new design decisions: sidebar layout, agent detail as full page, department-based KB output, template onboarding.

[2026-03-31] Agent Task Inbox system (PRD Section 3.4 + Phase 3). Built structured agent-to-agent task handoffs: task-inbox.ts manager (create, read, update tasks stored as JSON files per agent in tasks/ directory), API route at /api/agents/tasks (GET tasks by agent or all, POST to create/update). Wired into heartbeat: agents see pending tasks in context, can create tasks via TASK_CREATE directive and mark them done via TASK_COMPLETE — task creation auto-announces in Agent Slack. Agent detail panel shows Task Inbox section with status icons (pending/in-progress/completed/failed), priority badges (P0-P3 color-coded), sender attribution with emoji, and "Start" action button. Agent cards show blue inbox badge with pending task count. Seeded realistic task data: Content Agent has 2 tasks (LinkedIn posts from Head of Marketing, blog repurpose from Research Analyst), Outreach Agent has lead scoring task from Content Agent, Founding Engineer has P0 architecture review from CEO. AgentTask type exported from types/agents.ts.
